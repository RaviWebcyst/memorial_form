
const CandleButton = () => {

  return (
     <button type="submit" className="btn w-100" style={{ backgroundColor: "#a16207", color: "#fff", borderRadius: "50px" }}>
            Create Memorial
    </button>
  );
};

export default CandleButton;